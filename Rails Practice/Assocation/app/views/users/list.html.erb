<h1>Users#list</h1>
<p>Find me in app/views/users/list.html.erb</p>

<p>
	
	<% if @users && @users.size > 0 %>	
		<% @users.each do|u| %>
			<p>
				<%= u.id %>
				<%= u.email %>
				<%= link_to("Delete" , {:controller=>"users" , :action=>"delete" ,:id=>u.id}, method: :delete, data: {confirm: 'Are you Sure'} ) %>
				<%= link_to("Edit" , {:controller=>"Users", :action=>"edit" , :id=>u.id } ) %>
				<%= link_to("New User" , {:controller=>"users" , :action=>"new" } ) %>
			</p>
		<% end %>
	<% end %>
</p>
