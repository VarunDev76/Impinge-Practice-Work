<h1>UploadMaterials#index</h1>
<p>Find me in app/views/upload_materials/index.html.erb</p>
<% @selects.each do|e| %>
	<p>
		<%= e.id %>
		<%#= image_tag( "uploads/#{e.picture.original_filename}" :options) %> <br>
		<%#="#{e.picture.original_filename}" %> <br>
		<img src="uploads/#{e.picture.original_filename}" alt=""> <br>
	</p>
<% end %>